// // Using Linear conversion
	// if (temp >= 0) {
	// 	red = temp/50 * maxColor;
	// 	green = (50-temp)/50 * maxColor;
	// 	blue = 0;
	// }
	// else if (temp < 0) {
	// 	red = 0;
	// 	green = -(-50-temp)/50 * maxColor;
	// 	blue = -(temp)/50 * maxColor;
	// }

	// TRIG CONVERSION //
	// var pi = Math.PI;
	// var phase = 100;
	// red = maxColor * Math.cos(pi/(phase/3)*(tempC - phase/3));
	// green = maxColor * Math.cos(pi/(phase/3)*(tempC));
	// blue = maxColor * Math.cos(pi/(phase/3)*(tempC + phase/3));

	// if (red > maxColor) {
	// 	red = maxColor
	// }
	// if (green > maxColor) {
	// 	green = maxColor
	// }
	// if (blue > maxColor) {
	// 	blue = maxColor
	// if (!red || red < 0) {red = 0;}
	// if (!green || green < 0) {green = 0}
	// if (!blue || blue < 0) {blue = 0;}
	
	
	// red = parseInt(red);
	// green = parseInt(green);
	// blue = parseInt(blue);

	// console.log('Red: ' + red + ', Green: ' + green + ', Blue: ' + blue);